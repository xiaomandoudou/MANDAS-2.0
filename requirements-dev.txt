# 开发和测试依赖

# 核心测试框架
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0

# HTTP和WebSocket客户端
requests>=2.28.0
websocket-client>=1.5.0

# 数据库
psycopg>=3.1.0  # PostgreSQL适配器
psycopg-binary>=3.1.0  # PostgreSQL适配器（二进制版本，Windows兼容）
redis>=4.5.0

# 代码质量工具
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# 注意：docker-compose已从依赖中移除，因为GitHub Actions内置了docker compose命令
